// Contact/call detail reached from Voice Lab and Calls caller links.
// Reuses the pipeline lead-detail client page; admin-gated by middleware.
export { default } from "@/app/pipeline/leads/[id]/page"
export const dynamic = "force-dynamic"
